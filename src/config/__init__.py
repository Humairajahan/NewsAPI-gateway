"""
Entry point for the `config` module.
Handles configurations application-wide.
"""

from .settings import (
    DB_URL,
    ALGORITHM,
    APP_SECRET,
    APP_EXPIRES_IN,
    REFRESH_TOKEN_EXPIRES_IN,
)

